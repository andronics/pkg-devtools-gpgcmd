name="gpgcmd"
description="GPG Command Wrapper - Encryption and key management"
version="0.1.0"
method="stow"
collection="devtools"
fold=false

# Ignore documentation files
supported_os=("linux")
depends=()
requires_commands=("gpg" "gpg2")
ignore=(
    "README.md"
)
